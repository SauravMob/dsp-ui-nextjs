"use client"

import React from 'react'

export default function CreativeForm({
    isEdit,
    editData,
    userId,
    isAdmin
}: {
    isEdit: boolean,
    editData: CreativeType,
    userId: number,
    isAdmin: boolean
}) {
    return (
        <div>CreativeForm</div>
    )
}
